{
  "name": "resume-builder",
  "dependencies": {
    "-": "^0.0.1",
    "extract-json": "^0.2.0",
    "resume-cli": "^3.0.4"
  },
  "scripts": {
    "get-theme": "extract-json resume.json meta theme",
    "build": "resume export ./public/index.html"
  }
}
